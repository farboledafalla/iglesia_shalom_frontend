// Componentes
import { Layout } from '../../components/Layout';

export const MiembrosMinisterios = () => {
   return (
      <Layout>
         <div>Miembros - Ministerios</div>
      </Layout>
   );
};
